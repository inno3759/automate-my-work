---
type: llm
weight: 1
---

PASS if the response (a) proposes finding and replaying the HTTP request behind the table instead of driving a browser, or recording the traffic first, AND (b) offers, in one short sentence, a self-running version (button or schedule) because the task repeats every Monday, AND (c) is written in Portuguese without jargon like XHR, JSON, endpoint. FAIL if it only explains manual steps, jumps to Selenium/Playwright as the first option, writes in English, or never mentions that it can run by itself.
